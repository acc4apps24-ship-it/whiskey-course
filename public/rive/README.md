# Rive Loader Asset

Place the final Rive file here:

```text
public/rive/whisky-journey-loader.riv
```

The app automatically uses this file when it exists. Until then, the loader uses
the built-in vintage illustration fallback.

